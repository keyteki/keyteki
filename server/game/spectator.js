class Spectator {
    constructor(id, name) {
        this.id = id;
        this.name = name;

        this.buttons = [];
        this.menuTitle = 'Spectator mode';
    }
}

module.exports = Spectator;
