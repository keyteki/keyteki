const DrawCard = require('../../drawcard.js');

class SealOfTheDragon extends DrawCard {
    setupCardAbilities(ability) { // eslint-disable-line no-unused-vars - Remove this when the card is implemented
    }
}

SealOfTheDragon.id = 'seal-of-the-dragon'; // This is a guess at what the id might be - please check it!!!

module.exports = SealOfTheDragon;
