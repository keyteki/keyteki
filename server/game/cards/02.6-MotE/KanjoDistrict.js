const DrawCard = require('../../drawcard.js');

class KanjoDistrict extends DrawCard {
    setupCardAbilities(ability) { // eslint-disable-line no-unused-vars - Remove this when the card is implemented
    }
}

KanjoDistrict.id = 'kanjo-district'; // This is a guess at what the id might be - please check it!!!

module.exports = KanjoDistrict;
