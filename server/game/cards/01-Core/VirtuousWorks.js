const Card = require('../../Card.js');

class VirtuousWorks extends Card {
    setupCardAbilities(ability) { // eslint-disable-line no-unused-vars
    }
}

VirtuousWorks.id = 'virtuous-works'; // This is a guess at what the id might be - please check it!!!

module.exports = VirtuousWorks;
