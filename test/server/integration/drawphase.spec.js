describe('draw phase', function() {
    integration(function() {
        describe('the honor bid during the draw phase', function() {
            beforeEach(function() {
                this.setupTest({
                });
            });

            it('should prompt both players for a bid', function() {

            });

            it('should prompt the second player if the first player chooses first', function() {

            });

            it('should prompt the first player if the second player chooses first', function() {

            });

            it('should correctly transfer honor when the first player bids more than the second', function() {

            });

            it('should correctly transfer honor when the second player bids more than the first', function() {

            });

            it('should not transfer any honor when bot player bid the same amount', function() {

            });

            it('should draw the correct number of cards for both players', function() {

            });
        });
    });
});
