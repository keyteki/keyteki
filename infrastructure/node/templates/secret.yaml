apiVersion: v1
kind: Secret
metadata:
    name: keyteki-node-secret
    # prettier-ignore
    namespace: {{ .Values.namespace }}
type: Opaque
stringData:
    # prettier-ignore
    node_config: {{ .Values.nodeConfig | toJson | quote }}
